/// Enum used in order to indicate to existing app screens
enum AppScreen {
  profile,
  home,
  scoreboard,
  login,
  register,
  chat,
  talk,
  settings,
}

enum UserLevel { beginner, intermediate, advanced }
